# Sourced by bash and zsh rc files. Environment variables only — no aliases or
# functions. Keep this POSIX-sh compatible so both shells can load it.

# Put dotfiles-managed binaries (e.g. pi-init) on PATH.
export PATH="$HOME/dotfiles/pi/bin:$PATH"
